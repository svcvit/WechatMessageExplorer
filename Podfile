platform :macos, '10.15'  # 指定macOS平台和最低版本

target 'WechatMessageExplorer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

    pod 'SQLite.swift/SQLCipher'

end
